class Math:
    x=227
    
    def sampMethod(surya):  # we sholud pass one argument mustly with any name....otherwise through error
        print('print sampmethod....')
    
obj=Math()
obj.sampMethod()# we can call func like the way


Math.sampMethod(obj) # we can also call func like the way